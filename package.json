{
  "name": "protecther",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build:packages": "corepack pnpm --filter @protecther/contracts build && corepack pnpm --filter @protecther/shared build",
    "dev": "concurrently -n api,mobile -c blue,magenta \"corepack pnpm dev:api\" \"corepack pnpm dev:mobile\"",
    "dev:api": "corepack pnpm run build:packages && corepack pnpm --filter protecther-api dev",
    "dev:mobile": "corepack pnpm run build:packages && corepack pnpm --filter protecther-mobile start:dev",
    "build": "corepack pnpm run build:packages && corepack pnpm --filter protecther-api build",
    "typecheck": "corepack pnpm -r typecheck",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "concurrently": "9.1.2",
    "typescript": "5.9.2"
  },
  "version": "1.0.0"
}
